# Slack surface — placeholder

A Slack app onto the Superatom engine. Nothing built yet.

Likely shape: a Slack bot (Events API + `/ask`-style slash command) whose backend
connects to the hub as a `runtime` peer (see `../README.md` for the shared
protocol) and posts the answer back as a message / Block Kit card.
