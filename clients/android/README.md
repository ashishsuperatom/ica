# Android surface — placeholder

A native Android app onto the Superatom engine. Nothing built yet.

Likely shape: a Kotlin app with a chat/question screen; a WebSocket client that
connects to the hub as a `runtime` peer (see `../README.md` for the shared
protocol) and renders the answer (prose + table + figures).
