{
  "name": "superatom-vm",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "//dependencies": "The root imports none of these. The data seams the engine writes into each project home import them; they resolve through the node_modules link ica/workspace.ts creates there, and from this root when the state directory sits inside the repository.",
  "dependencies": {
    "@superatom/grounding": "workspace:*",
    "@superatom/introspect": "workspace:*",
    "@superatom/datasource-index": "workspace:*"
  },
  "scripts": {
    "start": "pm2 start ../ecosystem.config.cjs",
    "stop": "pm2 stop ../ecosystem.config.cjs",
    "restart": "pm2 restart ../ecosystem.config.cjs",
    "status": "pm2 status",
    "logs": "pm2 logs",
    "flush": "pm2 flush",
    "dev": "pnpm run --parallel --filter datasource-manager --filter datasource-demo --filter code-engine dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check:generic": "tsx scripts/platform-says-nothing-about-one-dataset.mts",
    "check": "pnpm run typecheck && pnpm run check:generic"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "node-pty",
      "protobufjs",
      "@google/genai",
      "opencode-ai",
      "@openai/codex"
    ]
  },
  "//devDependencies": "The typecheck belongs to the workspace, not to one app inside it: tsconfig.json at this root covers apps/ and packages/, so the compiler and the Node types have to resolve from here.",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
